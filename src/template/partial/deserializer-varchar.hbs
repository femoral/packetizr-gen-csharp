{{~#if isArray~}}
model.{{name.pascalCaseName}}.Add(Encoding.ASCII.GetString(reader.ReadBytes(reader.ReadByte())));
{{~else~}}
model.{{name.pascalCaseName}} = Encoding.ASCII.GetString(reader.ReadBytes(reader.ReadByte()));
{{~/if~}}

{{~#if isArray~}}
model.{{name.pascalCaseName}}.Add(Encoding.ASCII.GetString(reader.ReadBytes({{length}})));
{{~else~}}
model.{{name.pascalCaseName}} = Encoding.ASCII.GetString(reader.ReadBytes({{length}}));
{{~/if~}}
